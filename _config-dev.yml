domain:  "" # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: http://localhost:4000 # the base hostname and protocol for your site, e.g. http://example.com
